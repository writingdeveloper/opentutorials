extends ../layout
block content
    article
      h1=topic.title+'게시글을 삭제하시겠습니까?'
      - rid = encodeURIComponent(topic['@rid'])
      form(action='/topic/'+rid+'/delete' method='post')
        p
          input(type='submit' value='YES')
          input(type='button' value='NO' onclick='location.href="/topic"')
