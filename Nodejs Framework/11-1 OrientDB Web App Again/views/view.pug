doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/topic') 심플게시판
    ul
      each topic in topics
        li
          - rid = encodeURIComponent(topic['@rid'])
          a(href='/topic/'+rid)=topic.title
    article
      if topic
        h2= topic.title
        = topic.description
        div= 'by '+topic.author
      else
        h2 심플 게시판
        | 심플 게시판에 오신것을 환영합니다.
    li
      a(href='/topic/add') 글작성
    if topic  
      - rid = encodeURIComponent(topic['@rid'])
      li  
        a(href='/topic/'+rid+'/edit') 수정 
      li  
        a(href='/topic/'+rid+'/delete') 삭제
